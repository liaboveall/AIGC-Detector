# Track 5：AI 生成图像鲁棒检测 — 宏观实施计划

## 版本说明

本文档为第二版宏观计划。初版（含大量代码细节）经过深度评审，发现数据地基与赛题认知存在根本性错误（详见第 2 节红线清单）。本版按评审结论全面修订，定位为**宏观路线图**：只定方向、红线与里程碑，不锁死细粒度技术选型，不含代码；所有具体实现（候选取舍、超参、脚本）推迟到细化阶段逐节确定。

---

## 目录

1. 赛题理解（修正版）
2. 红线与陷阱清单
3. 总体技术方向
4. 评估与迭代框架
5. 评分维度叙事规划
6. 交付物清单与规划
7. 阶段里程碑
8. 待细化决策清单
9. 主要风险概览
10. 附：与初版主要差异速查

---

## 1. 赛题理解（修正版）

### 1.1 核心任务

给定一张图片，输出它是 AI 生成图像的置信度分数（0~1，1 表示确定是 AI 生成）。
考核重点不是干净图上的精度，而是图像经过**压缩、模糊、缩放、噪声等变换后**的检测鲁棒性——
对应真实社交平台转发、再压缩的场景。

### 1.2 评分维度（官方 5 维度，评审制）

| 维度 | 权重 | 说明 |
|------|------|------|
| Technical Execution | 35% | 技术方案的实现质量与实验严谨性 |
| Innovation & Problem Insight | 20% | 创新性与对问题本质的洞察 |
| Impact & Relevance | 20% | 对真实场景（社交平台内容审核）的价值 |
| Feasibility & Practicality | 15% | 可落地性：算力成本、推理效率 |
| Presentation & Communication | 10% | 决赛路演与材料表达 |

**关键认知修正**：这是**评审制比赛**，不是数值竞赛——没有隐藏测试集、没有
AUC 公式、没有排行榜。鲁棒性指标是支撑评审叙事的证据，而非得分本身。
五个维度都要有对应的材料去"打动评审"，任何一维缺位都会直接丢分。

### 1.3 官方硬约束

1. **模型参数量 < 2B**（唯一硬性技术约束，选型与报告均需显式声明）。
2. **官方演示验证集严禁用于训练**：COCO val2017（4998 张）+ DALL·E Advanced
   （8843 张），仅用于演示与迭代追踪，报告中需明确声明未使用其训练。
3. **提交格式**：推理脚本输入图片目录，输出 JSON，每条记录含
   `image_path` 与 `pred` 两个字段；`pred` 必须在 0~1 区间内。
4. **变换考核是清单的"一个子集"**：官方列出变换清单，实际只考核其中未公布的
   一个子集——不能只针对某几个变换过拟合。
5. **交付物共 5 项**（详见第 6 节），其中 Robustness Evaluation Summary 与
   Error Analysis Note 是**独立交付物**，不是报告里的一个小节。
6. **8 月 28 日 17:00–17:45（GMT+8）官方 webinar**（飞书会议），放题后公布更多
   细节，务必参加并据此校正计划。

### 1.4 评测变换清单（备考范围）

| 变换类型 | 参数范围 | 对模型的挑战 |
|----------|----------|--------------|
| JPEG 压缩 | quality 90 / 70 / 50 / 30 | 衰减高频伪影线索 |
| 高斯模糊 | σ = 0.5 / 1.0 / 2.0 | 抹平边缘与纹理 |
| 缩放 | 0.5× / 0.25× 后再上采样 | 破坏像素级特征 |
| 高斯噪声 | σ = 0.02 / 0.05 / 0.10 | 掩盖统计分布差异 |
| 色彩抖动 | 亮度/对比度/饱和度 ±20% | 改变色彩统计量 |
| 中心裁剪 | 80% | 丢失边缘信息 |

注意：JPEG 与模糊是对检测杀伤最大的扰动（机理即衰减高频合成线索），
训练与选模必须围绕"在这些扰动下仍然有效"展开。

---

## 2. 红线与陷阱清单（初版致命问题，禁止重犯）

| 编号 | 陷阱 | 规避原则 |
|------|------|----------|
| F1 | 以 CIFAKE 为训练主力：它是 32×32、单生成器（SD 1.4）数据集，上采样训练会让模型学到"上采样伪影=假"的捷径，对原生高分辨率 AI 图完全失效 | 主力必须是高分辨率、多生成器数据；CIFAKE 最多作消融对照 |
| F2 | 类别与数据源绑定（real=COCO、fake=CIFAKE）：验证 AUC 测的是"区分数据源"而非"区分真假"，属捷径分数 | 多来源混合划分 train/val；真假图分辨率与压缩历史必须对齐 |
| S1 | 依赖不存在的开源仓库下载数据，Day 1 直接报错 | 只用官方/可核实渠道（Kaggle、ModelScope、官方链接），下载前先验证可达 |
| S2 | "JPEG 压缩让频域特征更明显"与文献相反：压缩/模糊恰是杀伤最大的扰动；全图 FFT 幅度谱对扩散模型与压缩均脆弱 | 叙事与选特征都以文献为准：相位谱、DCT 统计、像素级伪影与语义特征才是可靠方向 |
| S3 | 用干净图 AUC 选模：clean 最高的模型往往最过拟合高频伪影，变换后掉分最狠 | 选模准则 = 变换后鲁棒性指标（均值兼顾最差值） |
| S4 | TTA 叠加退化变换（再压缩/加噪）会把样本推离被评测分布；且 TTA 从未真正接入推理脚本 | TTA 只允许确定性变换（翻转、裁剪类），逐项验证不降分才启用，并真实接入 |
| S5 | 频谱特征逐样本 min-max 归一化会抹掉全局能量斜率这一关键判别信号 | 频域类特征用固定常数归一化，保留图间统计差异 |
| S6 | 推理输出 -1.0 越界（要求 0~1）且格式与官方字段不符 | 异常回退中性值 0.5；输出严格为 `image_path` + `pred` 的 JSON |

通用纪律：一切本地指标都要先回答"它测的是真假之别，还是数据源之别"；
一切对外宣称都要有文献或实验证据支撑。

---

## 3. 总体技术方向（宏观）

### 3.1 数据策略

**方向**：多生成器 + 高分辨率为主力，真假图分布对齐，官方推荐数据源优先。

- **fake 主力（候选，取舍待定）**：
  - WildFake（ModelScope，多生成器、含较新生成器）——覆盖面广，但体量大、下载可能慢；
  - GenImage（HuggingFace，取 2–3 个生成器子集如 Midjourney / SD 系列）——结构规整、易采样，但生成器覆盖面需自行搭配。
- **fake 补充**：SID_Set（官方推荐，先抽样检查构成）；时间允许再考虑
  DRCT-2M 子集补充扩散系多样性。
- **real 主力**：COCO train2017 子集（**不是** val2017，后者是官方演示集）。
- **官方演示集**：单独存放、只用于展示迭代进步，任何管线不得读取它参与训练。
- **CIFAKE**：仅作为消融对照素材（用于论证"低分辨率单生成器数据有害"）。

**对齐纪律**：真假图须匹配分辨率与压缩历史；训练中统一施加随机压缩增强，
避免模型学到"假图=某种压缩痕迹"的捷径。数据准备阶段须打印分辨率/格式分布
统计并人工确认对齐。

### 3.2 模型方向

**核心思路：双证据融合。** 语义主干提供跨生成器泛化能力，像素级伪影分支
提供"这张图有生成痕迹"的直接证据，两分支独立编码后融合，便于消融归因。

- **主干（候选，待定）**：
  - CLIP 预训练 ViT-B（timm / open_clip）：文献共识是 CLIP 特征空间为跨生成器
    泛化关键，受损/转发数据上更鲁棒；参数约 86M，远低于 2B 上限。
  - ImageNet 预训练 ViT：获取更简单、基线可比性强，但泛化预期弱于 CLIP。
- **伪影证据副分支（候选，待定）**：
  - NPR 式邻域像素差分支（CVPR24）：实现极简、对多种生成器有效、对压缩有
    一定韧性，是低风险首选；
  - 改良频域分支（相位谱 / DCT 统计 / patch 级频谱，固定常数归一化）：
    叙事契合赛题，但文献证据弱于像素差方案，且实现更繁琐。
  - 两者不互斥，细化阶段可先跑首选、有余力再对比。
- **参数预算**：任何组合总参数必须 < 2B 并在报告中声明（当前候选均 ~90M 量级）。

### 3.3 训练原则

1. **退化增强**：训练时系统性施加 JPEG、缩放、模糊、噪声增强，覆盖考核清单。
2. **转发链模拟**：多级串联压缩（压缩→缩放→再压缩）模拟社交平台转发链路，
   直接呼应 Impact / Feasibility 叙事。
3. **鲁棒性驱动选模**：训练循环内嵌轻量鲁棒性评估（每轮抽样跑 2–3 个代表变换），
   以变换后 AUC 均值（兼顾最差值）保存最优 checkpoint，而非 clean AUC。
4. **训练工程**：无死代码、解冻主干时重建优化器并加短 warmup、梯度裁剪、
   混合精度新式 API——细化阶段落实到代码。

### 3.4 推理原则

- **确定性优先**：推理链路（含任何 TTA）必须完全确定性、可复现。
- **格式合规**：输出官方 `image_path` + `pred` JSON；异常图片回退 0.5；
  支持递归扫描子目录；提交前用含损坏文件的样例目录做终验。

---

## 4. 评估与迭代框架

### 4.1 双轨评估

| 轨道 | 数据 | 用途 | 性质 |
|------|------|------|------|
| 官方演示集轨道 | COCO val2017 + DALL·E Advanced | 展示每次迭代的进步曲线 | 合规展示，**不用于选模** |
| 自建验证轨道 | 从训练数据源中留出的 held-out 子集（多来源混合） | 鲁棒性表格，真实选模依据 | 决策依据 |

自建鲁棒性表：对全部备考变换逐项评测，所有变换**确定性化**（随机扰动固定种子），
变换施加的分辨率与库实现做出明确假设并在报告中声明（官方允许
"reasonable assumptions as long as stated clearly"）。

### 4.2 迭代循环（概念）

1. 跑鲁棒性评估，找出掉分最多的弱点变换；
2. 针对性提升对应增强的概率 / 调整策略，重新训练；
3. 对比前后鲁棒性表，确认改进后记录并进入下一轮。

- **Day 1 基线**：先跑现成开源检测器的零样本结果（候选：AIGCDetectBenchmark
  预训练模型、UnivFD、NPR 官方权重），比从零训练更快建立参照系。
- **TTA 取舍**：仅限确定性变换（水平翻转、多尺度中心裁剪类），每项须在
  鲁棒性表上验证"不降分才启用"，并真实接入推理脚本。
- **集成取舍**：自训模型 + 现成检测器打分加权是可选增益，但必须在报告中
  论证推理成本合理性（或提供快速档/精确档双模式），否则不做。

---

## 5. 评分维度叙事规划

### 5.1 Impact & Relevance（20%）——社交平台内容审核

叙事要点：
- 场景锚定 TikTok 类平台：图像经历转发链压缩、缩略图化，检测器必须在
  "受损图像"上工作——这正是赛题变换的意义；
- 误报代价分析：false positive 会把创作者的真实作品误标为 AI，
  伤害创作生态——因此强调稳健阈值（阈值策略待定，见待细化决策 D10）与可解释性；
- 技术呼应：转发链模拟增强、确定性推理都是为该场景专门设计。

### 5.2 Feasibility & Practicality（15%）——推理成本论证

叙事要点：
- 单图毫秒级推理、12GB 消费级显卡即可训练与部署；
- 模型 ~90M 参数，远低于 2B 上限，留出巨大裕量；
- 若采用集成/TTA，提供"快速档/精确档"双模式并给出各自的吞吐数据。

### 5.3 Innovation & Problem Insight（20%）——创新叙事

核心主张（替代初版站不住的"频域更明显"叙事）：
1. **鲁棒性驱动的模型选择**：选模准则与赛题考核目标对齐；
2. **多证据融合**：语义证据（跨生成器泛化）+ 像素级伪影证据（生成痕迹）；
3. **转发链模拟**：训练分布与真实平台分发链路对齐。

---

## 6. 交付物清单与规划

| # | 交付物 | 内容方向 |
|---|--------|----------|
| 1 | Devpost 项目描述 | 问题洞察 + 方法亮点 + 关键数字（鲁棒性均值/最差值）+ 场景叙事 |
| 2 | 结构良好的 GitHub 仓库 | README 必须含：<2B 参数声明、假设清单、局限性与改进方向反思、复现步骤、团队贡献；代码与权重组织清晰 |
| 3 | Demo 视频 | 官方只要求 "a short video"；规划 3 分钟左右：问题展示 → 方法核心 → 鲁棒性动态演示 → 结果表 |
| 4 | Robustness Evaluation Summary（独立） | 完整变换鲁棒性表、迭代进步曲线（官方演示集）、选模日志、假设声明 |
| 5 | Error Analysis Note（独立） | 典型误报/漏报案例图、失败归因（风格/压缩历史/生成器）、阈值取舍讨论 |

---

## 7. 阶段里程碑（宏观）

### Day 1：基线与数据

- 环境就绪（12GB GPU + 混合精度验证通过）；
- 现成开源检测器零样本基线跑通（官方演示集 + 自建留出集各一份表）;
- 主力数据（多生成器 fake + COCO train2017 real）下载完成，
  分辨率/格式分布审计通过，train/val 混合划分完成；
- 评估变换管线实现，基线鲁棒性表产出；
- 首轮训练跑通，确认吞吐与显存。

**完成标志**：基线鲁棒性表 + 第一批 checkpoint + 数据统计报告。

### Day 2：训练与迭代（核心日）

- 完整训练（鲁棒性驱动选模）产出主模型；
- 弱点变换定位 → 针对性增强 → 重训，至少完成一轮完整迭代循环；
- 视余量：集成实验（自训 + 现成检测器，验证集搜权重）；
- 官方演示集迭代进步曲线同步记录。

**完成标志**：最终模型定型 + 两轨评估结果齐备。

### Day 3：收尾与提交

- 提交格式终验（含损坏文件目录测试，确认无越界值）；
- 可视化素材（注意力热图 / 频谱对比）；
- Demo 视频录制；
- Devpost 描述、Robustness Summary、Error Analysis Note 成稿；
- README 打磨（局限性、假设、复现步骤）、仓库整理与提交。

**完成标志**：5 项交付物全部就绪并通过清单核对。

---

## 8. 待细化决策清单（细化阶段逐项确定）

| # | 决策点 | 候选 | 决定时机 |
|---|--------|------|----------|
| D1 | fake 主力数据源 | WildFake / GenImage 子集 / 二者混合 | Day 1 上午，按下载速度与构成抽样结果 |
| D2 | 主干预训练 | CLIP ViT-B / ImageNet ViT-B | Day 1 训练前 |
| D3 | 副分支方案 | NPR 像素差 / 改良频域 / 双副分支 | Day 1–2，按消融结果 |
| D4 | 鲁棒性选模的代表变换与权重 | jpeg/blur/scale 组合；均值与最差值的权衡系数 | Day 1 评估管线实现时 |
| D5 | 是否启用集成及权重 | 自训 + NPR / UnivFD 打分加权 | Day 2 中段，视增益与成本 |
| D6 | 是否启用 TTA 及具体项 | 水平翻转 / 多尺度中心裁剪（逐项验证） | Day 2，以鲁棒性表为准 |
| D7 | 增强超参（概率、强度、转发链级数） | 按弱点变换迭代调整 | Day 2 迭代循环中 |
| D8 | 评测变换的具体实现假设 | 库选择（PIL/OpenCV）、施加分辨率、顺序 | Day 1，写入报告假设清单 |
| D9 | SID_Set / DRCT-2M 是否补充 | 视时间与构成 | Day 1–2 |
| D10 | 提交阈值策略 | 0.5 固定 / 验证集调优 | Day 3 终验时 |

---

## 9. 主要风险概览

| 风险 | 影响 | 预案 |
|------|------|------|
| 主力数据下载失败或过慢（WildFake 体量大） | Day 1 卡死 | 切换 GenImage 子集 / 先用 SID_Set 起步，后台续传 |
| 官方演示集获取方式不明确 | 迭代曲线缺失 | webinar 确认渠道；先用自建集替代叙事 |
| 新生成器泛化不足（测试子集含未见生成器） | 鲁棒性表与实际表现脱节 | 数据混入 2024 后生成器；报告中如实声明局限 |
| 变换子集实现与官方不一致 | 本地表格失真 | 假设清单明确声明（官方允许）；覆盖全部备考变换而非押题 |
| 12GB 显存吃紧 | 训练中断 | 混合精度 + 梯度累积 + 降 batch，优先保通 |
| Day 2 时间不足，两轮训练跑不完 | 迭代缺失 | 砍集成/第二副分支，保住"训练→评估→一次定向重训"主链 |
| 评审口径在 webinar 后变化 | 计划偏离 | Day 1 当晚对齐 webinar 信息，预留调整缓冲 |

---

## 附：与初版的主要差异（速查）

1. 评分体系改为官方 5 维度，明确评审制性质，补齐 Impact 维度（初版完全遗漏）。
2. 主力数据从 CIFAKE 换为多生成器高分辨率数据源；官方演示集转为"只展示、禁训练"。
3. 选模准则从 clean AUC 改为鲁棒性驱动；TTA 收窄为确定性变换并真实接入。
4. 推理输出改为官方 JSON 格式，异常回退 0.5。
5. Robustness Summary 与 Error Analysis Note 升级为独立交付物。
6. 全部细粒度技术决策（数据源/主干/副分支/集成/超参）降级为"候选 + 待定"，
   在细化阶段确定（见第 8 节决策清单）。
